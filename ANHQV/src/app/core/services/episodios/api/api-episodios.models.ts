import { CharacterEpisodios } from "../../characters/api/api-character.models";

//DATOS DE LA API EPISODIOS
export interface ApiEpisodios {
    title: CharacterEpisodios;
    description: string;
    video: string;
    id: string;
}
